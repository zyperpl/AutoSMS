<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="fill_parent"
        android:layout_height="46dp"
        android:background="@drawable/contact_style"
        android:clickable="true"
        android:focusable="true"
        android:paddingStart="4dp"
        android:paddingEnd="16dp">

    <ImageView
            android:id="@+id/contact_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="1dp"
            android:layout_weight="60"
            android:contentDescription="@string/contact_icon_desc"
            tools:srcCompat="@drawable/default_contact_image" />

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="30"
            android:orientation="vertical"
            android:padding="2dp">

        <TextView
                android:id="@+id/contact_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_weight="1"
                android:text="John Smith"
                android:textAlignment="gravity"
                android:textAllCaps="true"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="6pt"
                tools:ignore="HardcodedText,NestedWeights"

                />

        <TextView
                android:id="@+id/contact_number"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_weight="1"
                android:text="769-166-213"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="6pt"
                tools:ignore="HardcodedText" />
    </LinearLayout>

    <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/contact_selection_tick"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="60"

            android:contentDescription="@string/element_selected"
            app:srcCompat="@drawable/ic_tick" />
</LinearLayout>